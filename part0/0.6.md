sequenceDiagram
    participant browser
    participant server

    Note over browser: User types a note in the SPA input field and presses "Save"

    browser->>server: POST /exampleapp/new_note_spa<br/>Body: { content: "...", date: "..." }
    activate server
    server-->>browser: 201 Created<br/>Response: { saved: true }
    deactivate server

    Note right of browser: SPA logic updates the notes array locally (no page reload)

    Note right of browser: The render function is triggered to redraw the updated list on the page
