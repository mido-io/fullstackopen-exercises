sequenceDiagram
    participant browser
    participant server

    Note over browser: User types a note and submits the form

    browser->>server: POST /new_note<br/>Form data: note="example"
    activate server
    server-->>browser: 302 Redirect → /notes
    deactivate server
    
    Note over browser: Browser automatically follows the redirect

    browser->>server: GET /notes
    activate server
    server-->>browser: HTML content for notes page
    deactivate server

    browser->>server: GET /main.css
    server-->>browser: CSS stylesheet

    browser->>server: GET /main.js
    server-->>browser: JavaScript file

    Note right of browser: main.js is executed → triggers a request for updated data

    browser->>server: GET /data.json
    activate server
    server-->>browser: JSON array of notes (including newly added one)
    deactivate server

    Note right of browser: Browser updates the DOM to display the refreshed notes list
