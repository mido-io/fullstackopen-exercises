sequenceDiagram
    participant browser
    participant server

    Note over browser: User opens the SPA version of the notes application

    browser->>server: GET /exampleapp/spa
    activate server
    server-->>browser: HTML layout for the SPA
    deactivate server

    browser->>server: GET /exampleapp/main.css
    server-->>browser: Stylesheet

    browser->>server: GET /exampleapp/spa.js
    server-->>browser: JavaScript responsible for SPA logic

    Note right of browser: spa.js runs and immediately requests the stored notes

    browser->>server: GET /exampleapp/data.json
    activate server
    server-->>browser: JSON array containing all existing notes
    deactivate server

    Note right of browser: Browser updates the page by rendering notes dynamically (no full reload)
